/**
 * Shared color tokens for the TUI. Ink <Text> accepts these as the `color` prop.
 */
export const theme = {
  primary: 'cyan',
  dim: 'gray',
  danger: 'red',
  success: 'green',
  warn: 'yellow',
  accent: 'magenta',
} as const;
